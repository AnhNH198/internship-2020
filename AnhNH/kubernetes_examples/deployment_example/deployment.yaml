apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-deployment
spec:
  selector:
    matchLabels:
      app: story-deployment
  replicas: 1
  template:
    metadata:
      labels:
        app: story-deployment
    spec:
      containers:
        - name: story-deployment
          image: academind/kub-data-demo
